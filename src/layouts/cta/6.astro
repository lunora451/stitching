---

---

<!-- ============================================ --><!--                    CTA                       --><!-- ============================================ -->
<section id="cta-2144">
  <div class="cs-container">
    <div class="cs-left-section">
      <span class="cs-topper">Contact Info</span>
      <h2 class="cs-title">Get In Touch</h2>
    </div>
    <div class="cs-right-section">
      <div class="cs-contact">
        <a class="cs-phone" href="tel:555-555-5555"> +1(0) 987 654 321 </a>
        <a class="cs-email" href="mailto:email@email.com">
          stitchography@gmail.com
        </a>
        <a class="cs-address" href="">
          5the Avenue, E 28th St, Brooklyn NY, 2300 USA
        </a>
        <div class="cs-social">
          <a
            href=""
            class="cs-social-link"
            target="_blank"
            aria-label="visit our instagram profile">Instagram</a
          >
          <a
            href=""
            class="cs-social-link"
            target="_blank"
            aria-label="visit our facebook profile">Facebook</a
          >
          <a
            href=""
            class="cs-social-link"
            target="_blank"
            aria-label="visit our linkedin profile">LinkedIn</a
          >
        </div>
      </div>
      <picture class="cs-picture">
        <source
          media="(max-width: 600px)"
          srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/MISC/wedding-rings.jpg"
        />
        <source
          media="(min-width: 601px)"
          srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/MISC/wedding-rings.jpg"
        />
        <img
          aria-hidden="true"
          loading="lazy"
          decoding="async"
          src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/MISC/wedding-rings.jpg"
          alt="wedding rings"
          width="576"
          height="107"
        />
      </picture>
    </div>
  </div>
</section>

<style lang="scss">
  /*-- -------------------------- -->
<---            CTA             -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0rem) {
    #cta-2144 {
      /* 60px - 100px on top */
      padding: clamp(3.75rem, 7.5vw, 6.25rem) calc(16 / 16 * 1rem)
        calc(64 / 16 * 1rem) calc(16 / 16 * 1rem);
      background-color: #1a1a1a;
      overflow: hidden;
      position: relative;

      .cs-container {
        width: 100%;
        max-width: calc(704 / 16 * 1rem);
        margin: auto;
        /* prevents padding from adding to height and width */
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: flex-end;
        /* 48px - 100px */
        gap: clamp(3rem, 6vw, 6.25rem);
      }

      .cs-left-section {
        width: 100%;
      }

      .cs-title {
        /* 80px - 120px */
        font-size: clamp(5rem, 12vw, 7.5rem);
        line-height: 1em;
        text-transform: uppercase;
        margin: 0;
        color: var(--bodyTextColorWhite);
      }

      .cs-right-section {
        width: 100%;
      }

      .cs-contact {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: flex-end;
        /* 8px - 12px */
        gap: clamp(0.5rem, 1vw, 0.75rem);
      }

      .cs-social {
        display: flex;
        flex-direction: row;
        gap: calc(16 / 16 * 1rem);
      }

      .cs-phone,
      .cs-email,
      .cs-address,
      .cs-social-link {
        /* 14px - 16px */
        font-size: clamp(0.875rem, 1.5vw, 1rem);
        text-decoration: none;
        line-height: 1.5em;
        text-align: right;
        margin: 0;
        color: var(--bodyTextColorWhite);
        transition: color 0.3s;

        &:hover {
          color: var(--primary);
        }
      }

      .cs-social-link {
        text-decoration: none;
        padding-right: calc(16 / 16 * 1rem);
        border-right: 1px solid var(--bodyTextColorWhite);
        transition: color 0.3s;

        &:hover {
          color: var(--primary);
        }

        &:last-of-type {
          padding-right: 0;
          border-right: none;
        }
      }

      .cs-picture {
        width: 100%;
        height: calc(120 / 16 * 1rem);
        margin-top: calc(24 / 16 * 1rem);
        box-sizing: border-box;
        display: block;
        position: relative;

        img {
          width: 100%;
          height: 100%;
          object-fit: cover;
          position: absolute;
          top: 0;
          left: 0;
        }
      }
    }
  }

  /* Tablet - 768px */
  @media only screen and (min-width: 48rem) {
    #cta-2144 {
      .cs-container {
        max-width: calc(1280 / 16 * 1rem);
        flex-direction: row;
        justify-content: space-between;
      }

      .cs-right-section {
        text-align: right;
        max-width: 45%;
        /* prevents flexbox from squishing it */
        flex: none;
      }

      .cs-picture {
        /* 80px - 107px */
        height: clamp(5rem, 10vw, 6.7rem);
      }
    }
  }
</style>
