---

---

<!-- ============================================ --><!--                Call To Action                --><!-- ============================================ -->
<section id="cta-2400">
  <div class="cs-container">
    <div class="cs-content">
      <h2 class="cs-title">Looking to tidy up your yard?</h2>
      <p class="cs-text">
        Contact Poop Patrol Stitches today for your free estimate and see why so
        many pet owners trust our team.
      </p>
      <a href="" class="cs-button-solid">Get Your Free Quote</a>
    </div>
    <!--Adjust the max width of the image on tablet to change its height if you need it to be shorter-->
    <img
      class="cs-graphic"
      src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Graphics/dog.svg"
      alt="dog graphic"
      loading="lazy"
      decoding="async"
      width="681"
      height="534"
    />
  </div>
</section>

<style lang="scss">
  /*-- -------------------------- -->
<---      Call To Action        -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0rem) {
    #cta-2400 {
      padding: var(--sectionPadding);
      background-color: #191005;
      overflow: hidden;
      position: relative;
      z-index: 1;

      &:before {
        content: "";
        width: 100%;
        height: 100%;
        opacity: 0.02;
        background-image: url(https://nyc3.digitaloceanspaces.com/csimages2/Images/Patterns/noise100.png);
        background-attachment: fixed;
        background-repeat: repeat;
        display: block;
        position: absolute;
        top: 0;
        left: 0;
        pointer-events: none;
        z-index: 420;
      }

      .cs-container {
        width: 100%;
        max-width: calc(1280 / 16 * 1rem);
        margin: auto;
        padding: 0;
        display: flex;
        flex-direction: column-reverse;
        align-items: center;
        gap: calc(48 / 16 * 1rem);
        position: relative;
        z-index: 1;
      }

      .cs-content {
        // set text align to left if content needs to be left aligned
        text-align: center;
        width: 100%;
        max-width: calc(457 / 16 * 1rem);
        overflow: hidden;
        display: flex;
        flex-direction: column;
        // centers content horizontally, set to flex-start to left align
        align-items: center;
      }

      .cs-title {
        text-align: inherit;
        text-wrap: balance;
        margin-bottom: calc(16 / 16 * 1rem);
        color: var(--bodyTextColorWhite);
        position: relative;
        z-index: 1;
      }

      .cs-text {
        max-width: calc(457 / 16 * 1rem);
        margin-bottom: calc(16 / 16 * 1rem);
        color: var(--bodyTextColorWhite);

        &:last-of-type {
          margin-bottom: calc(32 / 16 * 1rem);
        }
      }

      .cs-button-solid {
        //responsive gradient button color adapts to unique branding color changes
        --gradientmix: color-mix(in lab, var(--primary), black 49%);

        font-size: calc(16 / 16 * 1rem);
        line-height: clamp(calc(46 / 16 * 1rem), 5.5vw, calc(56 / 16 * 1rem));
        text-decoration: none;
        font-weight: 700;
        text-align: center;
        color: #fff;
        white-space: nowrap;
        margin: 0;
        min-width: calc(150 / 16 * 1rem);
        padding: 0 calc(28 / 16 * 1rem);
        background-color: var(--primary);
        background-image: -moz-linear-gradient(
          top,
          rgba(0, 0, 0, 0) 0%,
          var(--gradientmix) 99%
        );
        background-image: -webkit-linear-gradient(
          top,
          rgba(0, 0, 0, 0) 0%,
          var(--gradientmix) 99%
        );
        background-image: linear-gradient(
          to bottom,
          rgba(0, 0, 0, 0) 0%,
          var(--gradientmix) 99%
        );
        border-radius: calc(100 / 16 * 1rem);
        overflow: hidden;
        display: inline-block;
        position: relative;
        z-index: 1;

        &:before {
          content: "";
          width: 0%;
          height: 100%;
          border-radius: calc(4 / 16 * 1rem);
          background: var(--secondary);
          position: absolute;
          top: 0;
          left: 0;
          z-index: -1;
          transition: width 0.3s;
          opacity: 1;
        }

        &:hover {
          &:before {
            width: 100%;
          }
        }
      }

      .cs-graphic {
        width: 100%;
        /* changes to 914px at tablet */
        max-width: calc(457 / 16 * 1rem);
        height: auto;
        display: block;
      }
    }
  }

  /* Tablet - 768px */
  @media only screen and (min-width: 48rem) {
    #cta-2400 {
      min-height: calc(301 / 16 * 1rem);

      .cs-container {
        //padding:clamp(calc(60/16 * 1rem), 7.8vw, calc(120/16 * 1rem)) 0;
        flex-direction: row;
        align-items: flex-end;
        gap: 0;
      }

      .cs-content {
        text-align: left;
        width: 45%;
        max-width: calc(522 / 16 * 1rem);
        align-items: flex-start;
        position: absolute;
        left: 0;
        top: 50%;
        transform: translateY(-50%);
      }

      .cs-graphic {
        width: 50vw;
        max-width: calc(914 / 16 * 1rem);
        max-height: 100%;
        position: relative;
        left: 50%;
        z-index: 1;
      }
    }
  }

  /* Desktop - 1024px */
  @media only screen and (min-width: 64rem) {
    #cta-2400 {
      min-height: calc(450 / 16 * 1rem);

      .cs-graphic {
        margin-left: calc(-52 / 16 * 1rem);
      }
    }
  }
</style>
