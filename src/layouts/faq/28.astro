---

---

<!-- ============================================ --><!--                    FAQ                       --><!-- ============================================ -->
<section id="faq-254">
  <div class="cs-container">
    <div class="cs-content">
      <span class="cs-topper">FAQ’s</span>
      <h2 class="cs-title">Frequently Asked Questions</h2>
      <p class="cs-text">
        Lorem ipsum, dolor sit amet consectetur adipisicing elit. Consequatur
        veritatis quae explicabo a quia veniam temporibus aliquid? Quos maiores
        architecto a officia aliquid atque reiciendis quae? Voluptas maxime sit
        quae!
      </p>
    </div>
    <div class="cs-flex-group">
      <ul class="cs-faq-group">
        <!-- Active class added as default -->
        <li class="cs-faq-item active">
          <button class="cs-button">Can you help me?</button>
          <p class="cs-item-p">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sit
            convallis nunc neque, bibendum pulvinar vitae commodo velit. Proin
            diam tortor sed malesuada nunc, habitant. Dignissim ipsum porta
            enim, magna urna, quam.
          </p>
        </li>
        <li class="cs-faq-item">
          <button class="cs-button">How long will it take?</button>
          <p class="cs-item-p">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sit
            convallis nunc neque, bibendum pulvinar vitae commodo velit. Proin
            diam tortor sed malesuada nunc, habitant. Dignissim ipsum porta
            enim, magna urna, quam.
          </p>
        </li>
        <li class="cs-faq-item">
          <button class="cs-button">How much will it cost?</button>
          <p class="cs-item-p">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sit
            convallis nunc neque, bibendum pulvinar vitae commodo velit. Proin
            diam tortor sed malesuada nunc, habitant. Dignissim ipsum porta
            enim, magna urna, quam.
          </p>
        </li>
        <li class="cs-faq-item">
          <button class="cs-button">Covered by insurance?</button>
          <p class="cs-item-p">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sit
            convallis nunc neque, bibendum pulvinar vitae commodo velit. Proin
            diam tortor sed malesuada nunc, habitant. Dignissim ipsum porta
            enim, magna urna, quam.
          </p>
        </li>
        <li class="cs-faq-item">
          <button class="cs-button">How long are the appointments?</button>
          <p class="cs-item-p">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sit
            convallis nunc neque, bibendum pulvinar vitae commodo velit. Proin
            diam tortor sed malesuada nunc, habitant. Dignissim ipsum porta
            enim, magna urna, quam.
          </p>
        </li>
      </ul>
      <div class="cs-cta">
        <h3 class="cs-h3">Don’t see the answer you need?</h3>
        <p class="cs-cta-p">
          That’s ok. Just drop a message and we will get back to you ASAP.
        </p>
        <a href="" class="cs-button-solid">Contact us</a>
      </div>
    </div>
  </div>
</section>

<style lang="scss">
  /*-- -------------------------- -->
<---            FAQ             -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0em) {
    #faq-254 {
      padding: var(--sectionPadding);
      background: #fafbfc;

      .cs-container {
        width: 100%;
        /* changes to 1280px at tablet */
        max-width: calc(550 / 16 * 1rem);
        margin: auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        /* 48px - 64px */
        gap: clamp(3rem, 6vw, 4rem);
      }

      .cs-content {
        /* set text align to left if content needs to be left aligned */
        text-align: center;
        width: 100%;
        display: flex;
        flex-direction: column;
        /* centers content horizontally, set to flex-start to left align */
        align-items: center;
      }

      .cs-faq-group {
        padding: 0;
        margin: 0;
        margin-bottom: calc(48 / 16 * 1rem);
      }

      .cs-faq-item {
        list-style: none;
        border-bottom: 1px solid #dad9e3;
        transition: border-bottom 0.3s;

        &.active {
          border-bottom: 1px solid var(--primary);
          .cs-button {
            &:before {
              transform: rotate(180deg);
            }
            &:after {
              opacity: 0;
              transform: rotate(360deg);
            }
          }
          .cs-item-p {
            height: auto;
            /* 12px - 16px */
            margin-bottom: clamp(0.75rem, 1.3vw, 1rem);
            opacity: 1;
          }
        }
      }

      .cs-button {
        /* 16px - 20px */
        font-size: clamp(1rem, 2vw, 1.25rem);
        line-height: 1.2em;
        text-align: left;
        font-weight: bold;
        /* 12px - 16px */
        padding: clamp(0.75rem, 1.3vw, 1rem) 0;
        background: transparent;
        border: none;
        color: var(--headerColor);
        display: block;
        width: 100%;
        position: relative;

        &:hover {
          cursor: pointer;
        }

        &:before {
          /* plus sign */
          content: "";
          width: calc(12 / 16 * 1rem);
          height: calc(2 / 16 * 1rem);
          background: var(--headerColor);
          opacity: 1;
          position: absolute;
          display: block;
          top: 50%;
          right: calc(4 / 16 * 1rem);
          transition: transform 0.3s;
        }

        &:after {
          /* plus sign */
          content: "";
          width: calc(12 / 16 * 1rem);
          height: calc(2 / 16 * 1rem);
          background: var(--headerColor);
          opacity: 1;
          position: absolute;
          display: block;
          top: 50%;
          right: calc(4 / 16 * 1rem);
          transform: rotate(90deg);
          transform-origin: center;
          transition:
            opacity 0.5s,
            transform 0.3s;
        }
      }

      .cs-item-p {
        /* 14px - 16px */
        font-size: clamp(0.875rem, 1.5vw, 1rem);
        line-height: 1.5em;
        width: 90%;
        height: 0;
        margin: 0;
        opacity: 0;
        color: var(--bodyTextColor);
        /* clips the text so it doesn't show up */
        overflow: hidden;
        transition:
          opacity 0.3s,
          margin-bottom 0.3s;
      }

      .cs-cta {
        text-align: center;
        /* 48px - 64px top & bottom */
        /* 40px - 56px left & right */
        padding: clamp(3rem, 4.9vw, 4rem) clamp(2.5rem, 4vw, 3.5rem);
        background: var(--primaryLight);
        border-radius: calc(16 / 16 * 1rem);
        /* prevents padding from adding to width and height */
        box-sizing: border-box;
      }

      .cs-h3 {
        /* 25px - 31px */
        font-size: clamp(1.5625rem, 3vw, 1.9375rem);
        line-height: 1.2em;
        font-weight: bold;
        text-align: center;
        margin: 0;
        margin-bottom: calc(16 / 16 * 1rem);
        color: var(--headerColor);
      }

      .cs-cta-p {
        /* 16px - 20px */
        font-size: clamp(1rem, 2vw, 1.25rem);
        text-align: center;
        line-height: 1.5em;
        margin: 0;
        /* 32px - 48px */
        margin-bottom: clamp(2rem, 3.9vw, 3rem);
      }

      .cs-button-solid {
        font-size: calc(16 / 16 * 1rem);
        /* 46px - 56px */
        line-height: clamp(2.875rem, 5.5vw, 3.5rem);
        text-decoration: none;
        font-weight: 700;
        text-align: center;
        margin: 0;
        color: #fff;
        min-width: calc(150 / 16 * 1rem);
        padding: 0 calc(24 / 16 * 1rem);
        background-color: var(--primary);
        border-radius: calc(4 / 16 * 1rem);
        display: inline-block;
        position: relative;
        z-index: 1;
        /* prevents padding from adding to the width */
        box-sizing: border-box;

        &:before {
          content: "";
          position: absolute;
          height: 100%;
          width: 0%;
          background: #000;
          opacity: 1;
          top: 0;
          left: 0;
          z-index: -1;
          border-radius: calc(4 / 16 * 1rem);
          transition: width 0.3s;
        }

        &:hover {
          &:before {
            width: 100%;
          }
        }
      }
    }
  }

  /* Tablet - 768px */
  @media only screen and (min-width: 48em) {
    #faq-254 {
      .cs-container {
        max-width: calc(1080 / 16 * 1rem);
      }

      .cs-flex-group {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        /* 16px - 48px */
        gap: clamp(1rem, 3.5vw, 3rem);
      }

      .cs-faq-group {
        margin: 0;
      }

      .cs-cta {
        width: 38.5%;
        max-width: calc(413 / 16 * 1rem);
        /* prevents flexbox from squishing it */
        flex: none;
      }
    }
  }
</style><script>
  const faqItems = Array.from(document.querySelectorAll(".cs-faq-item"));
  for (const item of faqItems) {
    const onClick = () => {
      item.classList.toggle("active");
    };
    item.addEventListener("click", onClick);
  }
</script>
