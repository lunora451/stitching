---

---

<!-- ============================================ --><!--                     CTA                      --><!-- ============================================ -->
<section id="cta-2356">
  <div class="cs-container">
    <div class="cs-content">
      <span class="cs-topper">Subscribe</span>
      <h2 class="cs-title">
        Offering an array of services in the fields of architecture and interior
        design.
      </h2>
    </div>
    <form class="cs-form" name="Contact Form" method="post">
      <input
        class="cs-input"
        type="email"
        id="cs-email-2356"
        name="email"
        placeholder="Email Address"
      />
      <button class="cs-submit" type="submit">
        <img
          class="cs-icon"
          aria-hidden="true"
          src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/arrow-right-white.svg"
          decoding="async"
          alt="icon"
          width="24"
          height="24"
        />
      </button>
    </form>
  </div>
</section>

<style lang="scss">
  /*-- -------------------------- -->
<---            CTA             -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0rem) {
    #cta-2356 {
      padding: var(--sectionPadding);
      overflow: hidden;
      position: relative;
      z-index: 1;

      .cs-container {
        width: 100%;
        max-width: calc(1800 / 16 * 1rem);
        margin: auto;
        /* 24px - 100px */
        padding: clamp(1.5rem, 3vw, 6.25rem);
        background-color: #f1eeec;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: flex-start;
        /* 48px - 64px */
        gap: clamp(3rem, 7vw, 4rem);
      }

      .cs-title {
        margin: 0;
        max-width: calc(854 / 16 * 1rem);
      }

      .cs-form {
        width: 100%;
        max-width: calc(846 / 16 * 1rem);
        padding-bottom: calc(16 / 16 * 1rem);
        display: flex;
        align-items: center;
        justify-content: center;
        /* 8px - 16px */
        gap: clamp(0.5rem, 2vw, 1rem);
        position: relative;
        z-index: 1;

        &:before {
          content: "";
          width: 100%;
          height: 1px;
          background: #a3a3a3;
          opacity: 0.8;
          position: absolute;
          display: block;
          bottom: 0;
          left: 0;
        }
      }

      .cs-input {
        font-size: calc(20 / 16 * 1rem);
        width: 100%;
        /* 60px - 72px */
        height: clamp(3.75rem, 5.5vw, 4.5rem);
        margin: 0;
        padding: 0;
        background-color: transparent;
        border: none;
        /* prevents padding from adding to width and height */
        box-sizing: border-box;
        display: block;

        &::placeholder {
          color: var(--bodyTextColor);
        }
      }

      .cs-submit {
        width: calc(56 / 16 * 1rem);
        height: calc(56 / 16 * 1rem);
        margin: 0;
        padding: 0;
        background-color: var(--primary);
        border-radius: 50%;
        border: none;
        display: flex;
        flex: none;
        justify-content: center;
        align-items: center;
      }

      .cs-icon {
        width: calc(24 / 16 * 1rem);
        height: auto;
        display: block;
      }

      .cs-floater {
        /* max width 167px */
        font-size: min(20vw, 10.4375rem);
        line-height: 1.5em;
        font-weight: 900;
        margin: 0;
        color: var(--primary);
        opacity: 0.05;
        display: block;
        position: absolute;
        left: 50%;
        top: 50%;
        transform: translate(-50%, -50%);
        z-index: -1;
      }
    }
  }

  /* Desktop - 768px */
  @media only screen and (min-width: 48rem) {
    #cta-2356 {
      .cs-container {
        flex-direction: row;
        align-items: flex-end;
        justify-content: space-between;
      }

      .cs-form {
        max-width: calc(618 / 16 * 1rem);
      }
    }
  }
</style>
