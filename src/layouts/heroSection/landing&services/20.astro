---

---

<!-- ============================================ --><!--                      Hero                    --><!-- ============================================ -->
<section id="hero-2269">
  <div class="cs-container">
    <div class="cs-content">
      <div class="cs-flex">
        <span class="cs-topper">Perfect Photography at Your Doorstep</span>
        <h2 class="cs-title">
          Chris Brown
          <img
            class="cs-img cs-img-1"
            src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/MISC/photography-hero-image-one.png"
            alt="posing"
            width="220"
            height="96"
            loading="eager"
            decoding="async"
          />
          the World Class
          <img
            class="cs-img cs-img-2"
            src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/MISC/photography-hero-image-two.png"
            alt="posing"
            width="220"
            height="96"
            loading="eager"
            decoding="async"
          />
          Wedding Photographer
        </h2>
      </div>
      <div class="cs-flex">
        <p class="cs-text">
          Authentic wedding photography emphasizes candid shots that capture
          real emotions, reactions, and interactions among the couple, their
          families, and guests. These moments are often spontaneous and
          heartfelt.
        </p>
        <a class="cs-button-solid" href="">Book An Appointment</a>
      </div>
    </div>
    <img
      class="cs-floater"
      src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/MISC/photography-hero-floater.png"
      alt="floating leaf"
      class="cs-floater"
      width="373"
      height="268"
      loading="eager"
      decoding="async"
    />
  </div>
</section>

<!-- ============================================ -->
<!--                    Services                  -->
<!-- ============================================ -->

<section id="services-2269">
  <div class="cs-container">
    <ul class="cs-card-group">
      <li class="cs-item">
        <a href="" class="cs-link">
          <h3 class="cs-h3">
            <img
              class="cs-icon"
              src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/flower.svg"
              loading="lazy"
              decoding="async"
              alt="icon"
              width="32"
              height="32"
              aria-hidden="true"
            />
            The Ceremony
          </h3>
          <p class="cs-item-text">
            Choose from our range of Photo booth options, including open-air to
            suit your wedding theme and style.
          </p>
        </a>
      </li>
      <li class="cs-item">
        <a href="" class="cs-link">
          <h3 class="cs-h3">
            <img
              class="cs-icon"
              src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/meal.svg"
              loading="lazy"
              decoding="async"
              alt="icon"
              width="32"
              height="32"
              aria-hidden="true"
            />
            The Lunch
          </h3>
          <p class="cs-item-text">
            Choose from our range of Photo booth options, including open-air to
            suit your wedding theme and style.
          </p>
        </a>
      </li>
      <li class="cs-item">
        <a href="" class="cs-link">
          <h3 class="cs-h3">
            <img
              class="cs-icon"
              src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/photo.svg"
              loading="lazy"
              decoding="async"
              alt="icon"
              width="32"
              height="32"
              aria-hidden="true"
            />
            Photo Booth
          </h3>
          <p class="cs-item-text">
            Choose from our range of Photo booth options, including open-air to
            suit your wedding theme and style.
          </p>
        </a>
      </li>
    </ul>
  </div>
</section>

<style lang="scss">
  /*-- -------------------------- -->
<---           Hero             -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0rem) {
    #hero-2269 {
      padding: var(--sectionPadding);
      background-color: #1a1a1a;
      overflow: hidden;
      position: relative;

      .cs-container {
        width: 100%;
        max-width: calc(1720 / 16 * 1rem);
        margin: auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        /* 48px - 56px */
        gap: clamp(3rem, 3vw, 3.5rem);
      }

      .cs-flex {
        width: 100%;
      }

      .cs-content {
        /* set text align to center if content needs to be centrally aligned */
        text-align: left;
        width: 100%;
        display: flex;
        flex-direction: column;
        /* aligns content to the left. set to center to align centrally */
        align-items: flex-start;
        gap: calc(32 / 16 * 1rem);
      }

      .cs-topper {
        color: var(--secondary);
      }

      .cs-title {
        /* 39px - 120px */
        font-size: clamp(2.4375rem, 11vw, 7.5rem);
        line-height: 1.1em;
        text-transform: uppercase;
        letter-spacing: -2px;
        width: 100%;
        max-width: calc(1045 / 16 * 1rem);
        margin: 0;
        color: var(--bodyTextColorWhite);
      }

      .cs-text {
        /* 16px - 20px */
        font-size: clamp(1rem, 2vw, 1.25rem);
        margin-bottom: calc(32 / 16 * 1rem);
        color: var(--bodyTextColorWhite);
      }

      .cs-img-1 {
        /* 94px - 220px */
        width: clamp(5.875rem, 22vw, 13.75rem);
        /* 40px - 96px */
        height: clamp(2.5rem, 10vw, 6rem);
        object-fit: cover;
      }

      .cs-img-2 {
        /* 195px - 361px */
        width: clamp(12.1875rem, 48vw, 22.5625rem);
        /* 40px - 96px */
        height: clamp(2.5rem, 10vw, 6rem);
        object-fit: cover;
      }

      .cs-floater {
        /* 126px - 268px */
        width: clamp(7.875rem, 19vw, 16.75rem);
        height: auto;
        object-fit: contain;
        position: absolute;
        right: calc(-34 / 16 * 1rem);
        bottom: calc(-17 / 16 * 1rem);
      }

      .cs-button-solid {
        font-size: calc(16 / 16 * 1rem);
        font-weight: bold;
        line-height: calc(50 / 16 * 1em);
        text-align: center;
        text-transform: uppercase;
        text-decoration: none;
        width: auto;
        padding: 0 calc(30 / 16 * 1rem);
        background-color: var(--primary);

        // Transition Properties
        color: #000;
        display: inline-block;
        position: relative;
        z-index: 1;
        transition: color 0.3s;
        transition-delay: 0.1s;

        &:hover {
          color: #fff;
          &:before {
            width: 100%;
          }
        }

        &:before {
          content: "";

          //Transition properties
          width: 0;
          height: 100%;
          background-color: #000;
          opacity: 1;
          display: block;
          position: absolute;
          top: 0;
          left: 0;
          z-index: -1;
          transition: width 0.3s;
        }
      }
    }
  }

  /* Large Desktop - 1400px */
  @media only screen and (min-width: 87.5rem) {
    #hero-2269 {
      .cs-content {
        flex-direction: row;
        justify-content: space-between;
        align-items: flex-end;
        gap: calc(100 / 16 * 1rem);
      }

      .cs-floater {
        top: calc(5 / 16 * 1rem);
        right: 0;
      }
    }
  }

  /*-- -------------------------- -->
<---          Services          -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0rem) {
    #services-2269 {
      max-width: calc(1920 / 16 * 1rem);
      margin: auto;
      position: relative;
      z-index: 10;

      &:before {
        /* background color */
        content: "";
        width: 100vw;
        height: 100%;
        background: #1a1a1a;
        opacity: 1;
        display: block;
        position: absolute;
        top: 0;
        left: 50%;
        transform: translateX(-50%);
      }

      .cs-container {
        width: 100%;
        margin: auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        /* 48px - 64px */
        gap: clamp(3rem, 6vw, 4rem);
      }

      .cs-card-group {
        width: 100%;
        margin: 0;
        /* prevents padding from affecting height and width */
        box-sizing: border-box;
        /* 60px - 100px top & bottom */
        /* 16px - 100px left and right */
        padding: clamp(3.75rem, 7vw, 6.25rem) clamp(1rem, 4vw, 6.25rem);
        display: grid;
        grid-template-columns: repeat(12, 1fr);
        row-gap: calc(32 / 16 * 1rem);
        position: relative;
        z-index: 15;
      }

      .cs-item {
        text-align: left;
        list-style: none;
        width: 100%;
        box-sizing: border-box;
        grid-column: span 12;
        transition: background-color 0.3s;
      }

      .cs-link {
        text-decoration: none;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: flex-start;
        gap: calc(16 / 16 * 1rem);
      }

      .cs-icon {
        width: calc(32 / 16 * 1rem);
        height: auto;
      }

      .cs-h3 {
        /* 20px - 25px */
        font-size: clamp(1.25rem, 2vw, 1.5625rem);
        font-weight: 700;
        line-height: 1.2em;
        text-align: inherit;
        margin: 0;
        color: var(--bodyTextColorWhite);
        display: flex;
        align-items: center;
        gap: calc(8 / 16 * 1rem);
        transition: color 0.3s;
      }

      .cs-item-text {
        /* 14px - 16px */
        font-size: clamp(0.875rem, 1.5vw, 1rem);
        line-height: 1.5em;
        text-align: inherit;
        margin: 0;
        color: var(--bodyTextColorWhite);
        opacity: 0.8;
        transition:
          color 0.3s,
          opacity 0.3s;
      }
    }
  }

  /* Tablet - 768px */
  @media only screen and (min-width: 48rem) {
    #services-2269 {
      .cs-content {
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
        gap: calc(48 / 16 * 1rem);
      }

      .cs-title {
        margin: 0;
      }

      .cs-card-group {
        /* 32px - 60px */
        column-gap: clamp(2rem, 6vw, 3.75rem);
      }

      .cs-flex-group {
        width: 50%;
        /* prevents flexbox from squishing it */
        flex: none;
      }

      .cs-item {
        grid-column: span 4;
      }
    }
  }
</style>
