---

---

<!-- ============================================ --><!--                    Hero                      --><!-- ============================================ -->
<section id="hero-446">
  <div class="cs-container">
    <div class="cs-content">
      <span class="cs-tag">Tag Example</span>
      <h1 class="cs-title">Solutions we crafted to make human lives easier.</h1>
      <p class="cs-text">
        Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod
        tempor incididunt ut.
      </p>
    </div>
    <!--Stats Section-->
    <ul class="cs-card-group">
      <li class="cs-item">
        <span class="cs-number">18k+</span>
        <span class="cs-desc">Satisfied Customers in Bartown, MA</span>
      </li>
      <li class="cs-item">
        <span class="cs-number">5/5</span>
        <span class="cs-desc">Google and Yelp rating</span>
      </li>
      <li class="cs-item">
        <span class="cs-number">100%</span>
        <span class="cs-desc">Satisfaction guaranteed on every project</span>
      </li>
    </ul>
  </div>
  <!-- Background Image -->
  <picture class="cs-background">
    <source
      media="(max-width: 600px)"
      srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Hero/leaf-bg-m.jpg"
    />
    <source
      media="(min-width: 601px)"
      srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Hero/leaf-bg.jpg"
    />
    <img
      loading="lazy"
      decoding="async"
      src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Hero/leaf-bg.jpg"
      alt="leaf"
      width="1920"
      height="1000"
      aria-hidden="true"
    />
  </picture>
</section>

<style lang="scss">
  /*-- -------------------------- -->
<---           Hero             -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0rem) {
    #hero-446 {
      /* Centers tag */
      text-align: center;
      /* 144px - 300px - leaving extra space for the navigation */
      padding: clamp(9rem, 25.95vw, 19.75rem) calc(16 / 16 * 1rem) 0;
      padding-bottom: 0;
      position: relative;
      z-index: 1;

      .cs-background {
        width: 100%;
        height: 100%;
        display: block;
        position: absolute;
        top: 0;
        left: 0;
        z-index: -2;

        &:before {
          /* Overlay */
          content: "";
          width: 100%;
          height: 100%;
          background: #1a1a1a;
          mix-blend-mode: multiply;
          opacity: 0.4;
          position: absolute;
          display: block;
          top: 0;
          left: 0;
          z-index: 1;
          /* prevents the cursor from interacting with it */
          pointer-events: none;
        }

        img {
          position: absolute;
          top: 0;
          left: 0;
          height: 100%;
          width: 100%;
          object-fit: cover;
        }
      }

      .cs-container {
        width: 100%;
        max-width: calc(1280 / 16 * 1rem);
        margin: auto;
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        align-items: center;
        /* 80px - 120px */
        gap: clamp(5rem, 10vw, 7.5rem);
      }

      .cs-content {
        margin: auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }

      .cs-tag {
        /* 14px - 16px */
        font-size: clamp(0.875rem, 1.5vw, 1rem);
        /* 30px - 44px */
        line-height: clamp(1.875rem, 4vw, 2.75rem);
        text-align: center;
        text-transform: uppercase;
        /* 12px - 20px */
        padding: 0 clamp(0.75rem, 1.6vw, 1.25rem);
        /* 12px - 20px */
        margin-bottom: clamp(0.75rem, 2vw, 1.25rem);
        color: var(--bodyTextColorWhite);
        border: 1px solid var(--bodyTextColorWhite);
        background: rgba(250, 251, 252, 0.2);
        -webkit-backdrop-filter: blur(6px);
        backdrop-filter: blur(6px);
        border-radius: calc(30 / 16 * 1rem);
        /* prevents border from affecting height and width */
        box-sizing: border-box;
        display: inline-block;
      }

      .cs-title {
        /* 31px - 61px */
        font-size: clamp(1.9375rem, 6.4vw, 3.8125rem);
        font-weight: 700;
        line-height: 1.2em;
        text-align: center;
        max-width: calc(829 / 16 * 1rem);
        /* 16px - 24px */
        margin: 0 auto clamp(1rem, 4vw, 1.5rem) 0;
        color: var(--bodyTextColorWhite);
        position: relative;
      }

      .cs-text {
        /* 16px - 20px */
        font-size: clamp(1rem, 1.95vw, 1.25rem);
        line-height: 1.5em;
        text-align: center;
        width: 100%;
        /* 464px - 622px */
        max-width: clamp(29rem, 60vw, 38.785rem);
        margin: 0;
        color: var(--bodyTextColorWhite);
      }

      .cs-card-group {
        width: 100%;
        padding: 0;
        margin: 0;
        background-color: #fff;
        filter: drop-shadow(0px 24px 54px rgba(87, 107, 147, 0.12));
        /* 12px - 24px */
        border-radius: clamp(0.75rem, 2vw, 1.5rem) clamp(0.75rem, 2vw, 1.5rem) 0
          0;
        display: flex;
        justify-content: center;
        align-items: stretch;
        flex-wrap: wrap;
      }

      .cs-item {
        list-style: none;
        width: 50%;
        margin: 0;
        /* 16px - 48px top and bottom */
        /* 12px - 110px top and bottom */
        padding: clamp(1rem, 3.7vw, 3rem) clamp(0.75rem, 3vw, 6.875rem);
        /* prevents padding and border from affecting height and width */
        box-sizing: border-box;
        border-right: 1px solid #dcdcdd;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        &:nth-of-type(2) {
          border: none;
        }
        &:last-of-type {
          width: 100%;
          border-top: 1px solid #dcdcdd;
          border-right: none;
        }
      }

      .cs-number {
        /* 31px - 61px */
        font-size: clamp(1.9375rem, 4vw, 3.8125rem);
        line-height: 1.2em;
        font-weight: 700;
        text-align: center;
        color: var(--primary);
        margin: 0 0 calc(4 / 16 * 1rem) 0;
        display: block;
      }

      .cs-desc {
        /* 14px - 16px */
        font-size: clamp(0.875rem, 1.5vw, 1rem);
        line-height: 1.2em;
        font-weight: 400;
        text-align: center;
        max-width: calc(200 / 16 * 1rem);
        margin: 0;
        color: var(--bodyTextColor);
        display: block;
      }
    }
  }

  /* In-Between - 650px */
  @media only screen and (min-width: 40.625rem) {
    #hero-446 {
      .cs-card-group {
        flex-wrap: nowrap;
      }

      .cs-item {
        width: 33.33%;
        &:nth-of-type(2) {
          border-right: 1px solid #dcdcdd;
        }
        &:last-of-type {
          width: 33.33%;
          border: none;
        }
      }
    }
  }
</style>
