---

---

<!-- ============================================ --><!--                   Reviews                    --><!-- ============================================ -->
<section id="reviews-2290">
  <div class="cs-container">
    <div class="cs-content">
      <span class="cs-topper">testimonial</span>
      <h2 class="cs-title">What Our Clients Say</h2>
    </div>
    <ul class="cs-card-group">
      <li class="cs-item">
        <!--Yellow Quote-->
        <img
          class="cs-quote"
          aria-hidden="true"
          src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/yellow-quote.svg"
          decoding="async"
          alt="quote icon"
          width="120"
          height="99"
        />
        <h3 class="cs-header">
          The products they offer are top-notch, and has great efficiency
        </h3>
        <p class="cs-item-text">
          They work closely with clients to deliver cost-effective solutions and
          high-quality service.
        </p>
        <div class="cs-flex-group">
          <picture class="cs-picture">
            <img
              class="cs-profile"
              src="https://csimages2.nyc3.digitaloceanspaces.com/Images/People/construction-review1.jpg"
              decoding="async"
              alt="profile"
              width="50"
              height="50"
              aria-hidden="true"
            />
          </picture>
          <span class="cs-name">
            Evelyn Carter
            <span class="cs-job">CEO of TechVision</span>
          </span>
        </div>
      </li>
      <li class="cs-item">
        <!--Yellow Quote-->
        <img
          class="cs-quote"
          aria-hidden="true"
          src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/yellow-quote.svg"
          decoding="async"
          alt="quote icon"
          width="120"
          height="99"
        />
        <h3 class="cs-header">
          They’re all about innovation and are super quick to adapt to changes
        </h3>
        <p class="cs-item-text">
          They collaborate with clients to find cost-effective solutions that
          yield remarkable outcomes.
        </p>
        <div class="cs-flex-group">
          <picture class="cs-picture">
            <img
              class="cs-profile"
              src="https://csimages2.nyc3.digitaloceanspaces.com/Images/People/construction-review2.jpg"
              decoding="async"
              alt="profile"
              width="50"
              height="50"
              aria-hidden="true"
            />
          </picture>
          <span class="cs-name">
            Marcus Reed
            <span class="cs-job">MD of FutureWorks</span>
          </span>
        </div>
      </li>
      <li class="cs-item">
        <!--Yellow Quote-->
        <img
          class="cs-quote"
          aria-hidden="true"
          src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/yellow-quote.svg"
          decoding="async"
          alt="quote icon"
          width="120"
          height="99"
        />
        <h3 class="cs-header">
          They encourage open communication, and embrace creative thinking
        </h3>
        <p class="cs-item-text">
          They work with clients to discover economical choices that ensure
          outstanding results.
        </p>
        <div class="cs-flex-group">
          <picture class="cs-picture">
            <img
              class="cs-profile"
              src="https://csimages2.nyc3.digitaloceanspaces.com/Images/People/construction-review3.jpg"
              decoding="async"
              alt="profile"
              width="50"
              height="50"
              aria-hidden="true"
            />
          </picture>
          <span class="cs-name">
            Sophie Patel
            <span class="cs-job">CEO OF NextGen</span>
          </span>
        </div>
      </li>
      <li class="cs-item">
        <!--Yellow Quote-->
        <img
          class="cs-quote"
          aria-hidden="true"
          src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/yellow-quote.svg"
          decoding="async"
          alt="quote icon"
          width="120"
          height="99"
        />
        <h3 class="cs-header">
          They promote open discussions and foster a creative environment
        </h3>
        <p class="cs-item-text">
          They partner with clients to identify budget-friendly options that
          deliver excellent results.
        </p>
        <div class="cs-flex-group">
          <picture class="cs-picture">
            <img
              class="cs-profile"
              src="https://csimages2.nyc3.digitaloceanspaces.com/Images/People/construction-review4.jpg"
              decoding="async"
              alt="profile"
              width="50"
              height="50"
              aria-hidden="true"
            />
          </picture>
          <span class="cs-name">
            Oliver King
            <span class="cs-job">President of Creativa</span>
          </span>
        </div>
      </li>
    </ul>
  </div>
</section>

<style lang="scss">
  /*-- -------------------------- -->
<---          Reviews           -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0rem) {
    #reviews-2290 {
      /* centers the button */
      text-align: center;
      padding: var(--sectionPadding);
      /* clips the orbs from causing overflow issues */
      overflow: hidden;
      position: relative;
      z-index: 1;

      .cs-container {
        width: 100%;
        /* changes to 1280px at large desktop */
        max-width: calc(824 / 16 * 1rem);
        margin: auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        /* 48px - 64px */
        gap: clamp(3rem, 6vw, 4rem);
      }

      .cs-content {
        /* set text align to left if content needs to be left aligned */
        text-align: center;
        width: 100%;
        display: flex;
        flex-direction: column;
        /* centers content horizontally, set to flex-start to left align */
        align-items: center;
      }

      .cs-title {
        max-width: 20ch;
        margin: 0;
      }

      .cs-card-group {
        width: 100%;
        margin: 0;
        padding: 0;
        display: grid;
        grid-template-columns: repeat(12, 1fr);
        /* 8px - 20px */
        gap: clamp(0.5rem, 2vw, 1.25rem);
      }

      .cs-item {
        text-align: left;
        list-style: none;
        /* prevents padding from adding to height and width */
        box-sizing: border-box;
        /* 16px - 32px */
        padding: clamp(1rem, 2vw, 2rem);
        background-color: #fff;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: flex-start;
        grid-column: span 12;
        /* 16px - 24px */
        gap: clamp(1rem, 3vw, 1.5rem);
        position: relative;
        z-index: 1;
        transition: transform 0.3s;

        &::before {
          /* background color */
          content: "";
          width: 100%;
          height: 100%;
          background: var(--secondary);
          /* prevents the mouse from interacting it */
          pointer-events: none;
          opacity: 0.05;
          display: block;
          position: absolute;
          top: 0;
          left: 0;
          z-index: -1;
        }

        &::after {
          content: "";
          width: 0;
          height: calc(4 / 16 * 1rem);
          background: var(--primary);
          position: absolute;
          bottom: 0;
          left: 0;
          z-index: 1;
          transition: width 0.3s;
        }

        &:hover {
          transform: translateY(-0.5rem);

          &::after {
            width: 100%;
          }
        }
      }

      .cs-quote {
        width: calc(40 / 16 * 1rem);
        height: auto;
        display: block;
      }

      .cs-header {
        font-size: calc(20 / 16 * 1rem);
        font-weight: 700;
        line-height: 1.2em;
        margin: 0;
        /* 16px - 24px */
        padding-bottom: clamp(1rem, 3vw, 1.5rem);
        color: var(--headerColor);
        border-bottom: 1px solid #e7e7e8;
      }

      .cs-item-text {
        font-size: calc(16 / 16 * 1rem);
        line-height: 1.5em;
        margin: 0;
        color: var(--bodyTextColor);
      }

      .cs-flex-group {
        /* in case one card has more text than the other, this pushes up against the review text so the name and title are always at the bottom. Only works if parent is a flexbox */
        margin-top: auto;
        display: flex;
        justify-content: flex-start;
        align-items: center;
        /* 8px - 24px */
        gap: clamp(0.8rem, 1.5vw, 1rem);
      }

      .cs-profile {
        width: calc(60 / 16 * 1rem);
        height: calc(60 / 16 * 1rem);
        margin: 0;
        box-sizing: border-box;
        display: block;
        position: relative;

        img {
          width: 100%;
          height: 100%;
          /* makes the image behave like a background image */
          object-fit: cover;
          position: absolute;
          top: 0;
          left: 0;
        }
      }

      .cs-name {
        font-size: calc(16 / 16 * 1rem);
        font-weight: 700;
        line-height: 1.5em;
        margin: 0;
        color: var(--headerColor);
        display: block;
      }

      .cs-job {
        font-size: calc(16 / 16 * 1rem);
        font-weight: 400;
        line-height: 1.5em;
        margin: 0;
        color: var(--bodyTextColor);
        display: block;
      }
    }
  }

  /* Tablet - 768px */
  @media only screen and (min-width: 48rem) {
    #reviews-2290 {
      .cs-item {
        grid-column: span 6;
      }
    }
  }

  /* Large Desktop - 1300px */
  @media only screen and (min-width: 81.25rem) {
    #reviews-2290 {
      .cs-container {
        max-width: calc(1280 / 16 * 1rem);
      }

      .cs-item {
        grid-column: span 3;
      }
    }
  }
</style>
